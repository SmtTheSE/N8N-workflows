{
  "name": "AI Inbox Summarizer Bot",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "inbox-summary",
        "responseMode": "responseNode",
        "options": {}
      },
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [0, 0],
      "id": "webhook-inbox-001",
      "name": "Webhook Trigger",
      "webhookId": "inbox-summary-webhook"
    },
    {
      "parameters": {
        "jsCode": "return $input.all().map(item => {\n  const j = item.json;\n\n  // If messages already present (webhook path), use them; otherwise expect items[] from previous node\n  const messages = Array.isArray(j.messages) ? j.messages : ($input.all().map(i => i.json));\n\n  const normalized = messages.map((m, index) => ({\n    id: m.id || String(index + 1),\n    from: m.from || m.sender || \"unknown\",\n    subject: m.subject || m.title || \"(no subject)\",\n    body: m.body || m.text || \"\",\n    timestamp: m.timestamp || m.date || null\n  }));\n\n  return {\n    json: {\n      source: j.source || \"mixed\",\n      requester: j.requester || \"unknown\",\n      messages: normalized\n    }\n  };\n});\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [400, 0],
      "id": "code-normalize-001",
      "name": "Normalize Inbox Items"
    },
    {
      "parameters": {
        "promptType": "define",
        "text": "=You are an AI assistant creating a daily executive inbox summary from a batch of messages.\n\nReturn ONLY a valid minified JSON object with the following keys:\ndate (string, ISO date for this summary)\noverall_summary (string, max 200 words)\nhighlights (array of 3-10 short strings describing the most important items)\naction_items (array of objects with keys: description, owner_suggestion, due_date_iso, priority)\nrisks (array of short strings, can be empty)\n\nRules:\n- Never output markdown.\n- Never output explanation outside the JSON.\n- Use date = today's date in ISO (YYYY-MM-DD) if you are unsure.\n- priority must be one of: low, medium, high.\n- If there are no clear risks, return risks as an empty array.\n\nHere is the input inbox in JSON form:\n{{$json.messages}}",
        "messages": {
          "messageValues": [
            {
              "message": "=You are an AI assistant creating a daily executive inbox summary from a batch of messages.\n\nReturn ONLY a valid minified JSON object with the following keys:\ndate (string, ISO date for this summary)\noverall_summary (string, max 200 words)\nhighlights (array of 3-10 short strings describing the most important items)\naction_items (array of objects with keys: description, owner_suggestion, due_date_iso, priority)\nrisks (array of short strings, can be empty)\n\nRules:\n- Never output markdown.\n- Never output explanation outside the JSON.\n- Use date = today's date in ISO (YYYY-MM-DD) if you are unsure.\n- priority must be one of: low, medium, high.\n- If there are no clear risks, return risks as an empty array.\n\nHere is the input inbox in JSON form:\n{{$json.messages}}"
            }
          ]
        },
        "batching": {},
        "options": {
          "temperature": 0.2
        }
      },
      "type": "@n8n/n8n-nodes-langchain.chainLlm",
      "typeVersion": 1.9,
      "position": [750, 0],
      "id": "llm-summary-001",
      "name": "AI Daily Summary"
    },
    {
      "parameters": {
        "options": {}
      },
      "type": "@n8n/n8n-nodes-langchain.lmChatGoogleGemini",
      "typeVersion": 1,
      "position": [750, 200],
      "id": "gemini-model-001",
      "name": "Google Gemini Chat Model",
      "credentials": {
        "googlePalmApi": {
          "id": "google-gemini-cred",
          "name": "Google Gemini(PaLM) Api account"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "return $input.all().map(item => {\n  function findJsonString(obj) {\n    if (typeof obj === 'string' && obj.includes('{')) return obj;\n    if (obj && typeof obj === 'object') {\n      for (const k in obj) {\n        const found = findJsonString(obj[k]);\n        if (found) return found;\n      }\n    }\n    return null;\n  }\n\n  let raw = findJsonString(item.json) || \"\";\n  raw = raw.replace(/```json/g, \"\").replace(/```/g, \"\").trim();\n\n  let d;\n  try {\n    d = JSON.parse(raw);\n  } catch (e) {\n    d = {\n      date: new Date().toISOString().slice(0, 10),\n      overall_summary: \"Failed to parse AI output.\",\n      highlights: [],\n      action_items: [],\n      risks: []\n    };\n  }\n\n  const pick = (v, allowed, fallback) => allowed.includes(v) ? v : fallback;\n\n  const normalized = {\n    date: String(d.date || new Date().toISOString().slice(0, 10)),\n    overall_summary: String(d.overall_summary || \"No summary provided.\"),\n    highlights: Array.isArray(d.highlights) ? d.highlights.map(String) : [],\n    action_items: Array.isArray(d.action_items) ? d.action_items.map(a => ({\n      description: String(a.description || \"\"),\n      owner_suggestion: String(a.owner_suggestion || \"unassigned\"),\n      due_date_iso: a.due_date_iso || null,\n      priority: pick(a.priority || \"medium\", [\"low\",\"medium\",\"high\"], \"medium\")\n    })) : [],\n    risks: Array.isArray(d.risks) ? d.risks.map(String) : []\n  };\n\n  return { json: normalized };\n});\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1100, 0],
      "id": "code-validate-001",
      "name": "Validate AI Output"
    },
    {
      "parameters": {
        "chatId": "1895930444",
        "text": "={{ `📊 Daily Inbox Summary (` + $json.date + `)\n\n` + $json.overall_summary + `\n\nTop highlights:\n` + ($json.highlights[0] ? `- ${$json.highlights[0]}\n` : '') + ($json.highlights[1] ? `- ${$json.highlights[1]}\n` : '') + ($json.highlights[2] ? `- ${$json.highlights[2]}\n` : '') }}",
        "additionalFields": {}
      },
      "type": "n8n-nodes-base.telegram",
      "typeVersion": 1.2,
      "position": [1450, -150],
      "id": "telegram-notify-001",
      "name": "Telegram Notification",
      "webhookId": "telegram-inbox-webhook",
      "credentials": {
        "telegramApi": {
          "id": "telegram-cred",
          "name": "Telegram account"
        }
      }
    },
    {
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "log-success-001",
              "name": "status",
              "value": "logged",
              "type": "string"
            }
          ]
        },
        "options": {}
      },
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [1450, 150],
      "id": "log-success-001",
      "name": "Log Success"
    },
    {
      "parameters": {
        "options": {}
      },
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.5,
      "position": [1800, 0],
      "id": "respond-webhook-001",
      "name": "Respond to Webhook"
    }
  ],
  "pinData": {},
  "connections": {
    "Google Gemini Chat Model": {
      "ai_languageModel": [
        [
          {
            "node": "AI Daily Summary",
            "type": "ai_languageModel",
            "index": 0
          }
        ]
      ]
    },
    "Webhook Trigger": {
      "main": [
        [
          {
            "node": "Normalize Inbox Items",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize Inbox Items": {
      "main": [
        [
          {
            "node": "AI Daily Summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "AI Daily Summary": {
      "main": [
        [
          {
            "node": "Validate AI Output",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate AI Output": {
      "main": [
        [
          {
            "node": "Telegram Notification",
            "type": "main",
            "index": 0
          },
          {
            "node": "Log Success",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Telegram Notification": {
      "main": [
        [
          {
            "node": "Respond to Webhook",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Log Success": {
      "main": [
        [
          {
            "node": "Respond to Webhook",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": "inbox-summary-v1",
  "meta": {
    "templateCredsSetupCompleted": true
  },
  "id": "AI-Inbox-Summarizer-001",
  "tags": []
}